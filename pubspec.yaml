name: drinner_flutter
description: Pick meal or hanging out place together.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: 0.18.1
  flutter_map:
    git:
      url: https://github.com/tomwyr/flutter_map
  shared_preferences: 0.4.2
  http: ^0.12.0
  geolocator: ^2.1.0

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/food/
    - images/avatars/
